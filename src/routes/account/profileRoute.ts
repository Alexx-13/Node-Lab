import express, { Request, Response } from 'express'

const profileRouter = express.Router()

profileRouter.use("/", async (request: Request, response: Response) => {
    
})

export default profileRouter