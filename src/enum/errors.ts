export enum Errors {
    accessToken = 'Access token was not created',
    accessTokenRemote = 'Access token was not riched',
    refreshToken = 'Refreshtoken was not created',
    readLocalToken = 'Local tokens was not read',
    falseAdmin = 'Your role is not an admin'
}